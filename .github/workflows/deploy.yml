name: Deploy

# Builds and pushes the image to a registry, per GitHub Environment.
#
# Uses GitHub Environments so dev and production can have DIFFERENT registry
# config. Two environments (Settings → Environments): `develop` and
# `production` (environment names are decoupled from branch names). In EACH
# environment set:
#   vars.IMAGE_REGISTRY        e.g. vcr.vngcloud.vn/<namespace>
#   secrets.REGISTRY_USERNAME
#   secrets.REGISTRY_PASSWORD
# (Add production protection rules / required reviewers as desired.)
#
# Trunk-based triggers:
#   push to `main` touching any package's files (path filter below)
#        -> deploy to the `develop` environment   (image tag = <commit sha>)
#        builds every package in the matrix below
#   workflow_dispatch with tag input (chained from release-please, or manual)
#        -> deploy to the `production` environment (image tag = vX.Y.Z, component prefix stripped)
#        only the matrix entry whose component matches the released tag builds
#
# Notes:
# - If either environment restricts "Deployment branches and tags"
#   (Settings → Environments → <env>), it must allow `main` (develop env)
#   (develop env) and `main` (production env — dispatch runs on main).
# - Do NOT gate on `vars.IMAGE_REGISTRY` in a job-level `if` — environment
#   variables are not reliably available there; the "Show target" step fails
#   loudly instead when the environment is misconfigured.

on:
  # Production deploys run ONLY via workflow_dispatch with a tag input —
  # dispatched by the release-please workflow after a release, or manually.
  # (No tag push trigger: with the default GITHUB_TOKEN release tags never
  # fire push workflows, and with a PAT they would double-deploy.)
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag to deploy to production, e.g. vks-mcp-server-v0.5.0"
        required: true
        type: string
  push:
    branches: [main]
    paths:
      - 'src/vks-mcp-server/**'
      - 'src/agentbase-mcp-server/**'
      - 'src/mcp-core/**'
      - 'pyproject.toml'
      - 'uv.lock'
      - '.github/workflows/deploy.yml'

jobs:
  build-and-push:
    name: Build & push image (${{ matrix.component }})
    runs-on: ubuntu-24.04
    # tag push or tag-input dispatch -> production; main push -> develop
    environment: ${{ (startsWith(github.ref, 'refs/tags/') || inputs.tag != '') && 'production' || 'develop' }}
    permissions:
      contents: read
    timeout-minutes: 15
    strategy:
      fail-fast: false
      # One entry per released package that ships a Docker image. On a push
      # to main every entry builds; on a production dispatch
      # (workflow_dispatch with `tag`) only the entry whose component matches
      # the released tag runs — the others are skipped in-step via `if` below.
      matrix:
        component:
          - vks-mcp-server
          - agentbase-mcp-server
    steps:
      - uses: actions/checkout@v7
        with:
          # On tag-input dispatch, build the tagged source (not main)
          ref: ${{ inputs.tag || github.ref }}

      - name: Show target & verify environment config
        run: |
          echo "Environment: ${{ (startsWith(github.ref, 'refs/tags/') || inputs.tag != '') && 'production' || 'develop' }}"
          echo "Component:   ${{ matrix.component }}"
          echo "Registry:    ${{ vars.IMAGE_REGISTRY }}"
          if [ -z "${{ vars.IMAGE_REGISTRY }}" ]; then
            echo "::error::IMAGE_REGISTRY variable is empty for this environment. Set it in Settings → Environments → <env> → Variables."
            exit 1
          fi
          if [ -z "${{ secrets.REGISTRY_USERNAME }}" ] || [ -z "${{ secrets.REGISTRY_PASSWORD }}" ]; then
            echo "::error::REGISTRY_USERNAME / REGISTRY_PASSWORD secret is missing for this environment."
            exit 1
          fi

      # On a production dispatch, only the matrix entry whose component owns
      # the released tag does any real work; the others skip here and below.
      - name: Compute image tag & target
        id: meta
        if: ${{ github.event_name != 'workflow_dispatch' || startsWith(inputs.tag, format('{0}-v', matrix.component)) }}
        run: |
          COMPONENT="${{ matrix.component }}"
          # greennode-<short>-mcp naming: strip the trailing `-server` only,
          # keeping `-mcp` (vks-mcp-server -> greennode-vks-mcp).
          SHORT="${COMPONENT%-server}"
          IMAGE="greennode-${SHORT}"
          DOCKERFILE="src/${COMPONENT}/Dockerfile"
          echo "image=${IMAGE}"           >> "$GITHUB_OUTPUT"
          echo "dockerfile=${DOCKERFILE}" >> "$GITHUB_OUTPUT"
          if [ -n "${{ inputs.tag }}" ]; then
            REF_TAG="${{ inputs.tag }}"
            echo "tag=${REF_TAG#${COMPONENT}-}" >> "$GITHUB_OUTPUT"
          elif [ "${{ startsWith(github.ref, 'refs/tags/') }}" = "true" ]; then
            echo "tag=${GITHUB_REF_NAME#${COMPONENT}-}" >> "$GITHUB_OUTPUT"
          else
            echo "tag=${GITHUB_SHA::12}" >> "$GITHUB_OUTPUT"
          fi

      - name: Log in to registry
        if: ${{ github.event_name != 'workflow_dispatch' || startsWith(inputs.tag, format('{0}-v', matrix.component)) }}
        run: echo "${{ secrets.REGISTRY_PASSWORD }}" | docker login "${{ vars.IMAGE_REGISTRY }}" -u "${{ secrets.REGISTRY_USERNAME }}" --password-stdin

      - name: Build image
        if: ${{ github.event_name != 'workflow_dispatch' || startsWith(inputs.tag, format('{0}-v', matrix.component)) }}
        run: docker build -f "${{ steps.meta.outputs.dockerfile }}" -t "${{ vars.IMAGE_REGISTRY }}/${{ steps.meta.outputs.image }}:${{ steps.meta.outputs.tag }}" .

      - name: Push image
        if: ${{ github.event_name != 'workflow_dispatch' || startsWith(inputs.tag, format('{0}-v', matrix.component)) }}
        run: docker push "${{ vars.IMAGE_REGISTRY }}/${{ steps.meta.outputs.image }}:${{ steps.meta.outputs.tag }}"
